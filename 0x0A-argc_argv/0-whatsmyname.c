#include "main.h"
#include <stdio.h>

/**
 * main - return int
 * @args: length of the argument
 * @argv: char array of the argument u enter
 * Return: return int
 */

int main(int args, char *argv[])
{
	(void)args;

	printf("%s\n", argv[0]);

	return (0);
}
